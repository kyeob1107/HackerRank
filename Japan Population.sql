[Japan Population | HackerRank](https://www.hackerrank.com/challenges/japan-population/problem?isFullScreen=false)

일본도시의 총인구를 구하는 문제이다.

```sql
SELECT SUM(c.population)
FROM city c
WHERE c.countrycode = 'JPN'
```
